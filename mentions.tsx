import {View, Text, ScrollView} from 'react-native';
export default () => {
    return (
        <ScrollView>
        <Text>This is the Mentions component content.</Text>
        </ScrollView>
    );
    }